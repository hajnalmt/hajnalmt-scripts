# Arm interview postmortem: valid number

Date: 2026-07-10
Status: initial notes, to be continued

## What happened

The interview included a traditional live-coding task: implement an `is_valid_number` function that receives a string argument and returns whether the string is a valid number.

A function skeleton was provided. I started writing a `for` loop, but I froze on basic implementation details and did not get to a clean solution.

## What I failed on

- I did not think of using a regular expression during the interview.
- I started writing a manual character-by-character loop.
- I got stuck on the basic Python membership syntax for character checks: `not in`.
- The interviewer helped me with syntax.
- After that, the interviewer asked me to extend the solution with floating point support.
- Then they asked me to extend it again with exponent support using `e` / `E`.
- I did not think about optional signs, floating point formats, or exponent notation up front.
- I still did not remember the `not in` syntax afterward, which is a useful signal: my mind forgets easily googleable syntax under pressure.
- I did not remember `enumerate` for index-based loops.
- For sign handling, I thought of slicing off a leading minus with `s = s[1:]`, but did not think about a leading plus.

## Important interpretation

This was not a failure to understand software engineering.

This was a failure of live, memory-based, traditional coding performance under interview pressure.

The gap exposed here is not architectural judgment, code review ability, MLOps ability, or senior engineering taste. The exposed gap is:

- small-function implementation fluency,
- syntax recall under stress,
- interview-pattern recognition,
- and nervous-system tolerance for artificial live-coding tasks.

That is painful, but it is trainable.

## Why it felt bad

The task was simple enough that failing it felt humiliating.

The frustrating part is that, in real work, syntax like this is normally searched, checked in docs, generated by an assistant, or recovered quickly from existing code. Even ten years ago, googling syntax was normal.

The interview format made easily recoverable details feel like a measure of competence.

## What this task was probably testing

Whether I could:

- clarify the accepted number grammar,
- list valid and invalid examples,
- choose a parsing approach,
- implement a small validator,
- handle edge cases,
- and stay calm while doing it live.

The task was not really about the exact function. It was about the interview performance loop.

## Missing recovery pattern

A better response would have been:

1. Pause and clarify the accepted grammar.
2. Write examples before code.
3. List edge cases.
4. Choose either regex or a manual parser.
5. Implement a minimal solution slowly.
6. Test aloud against the examples.

For this problem, the examples should include:

| Input | Expected |
| --- | --- |
| `"0"` | true |
| `" 0.1 "` | true |
| `"abc"` | false |
| `"1 a"` | false |
| `"2e10"` | true |
| `"-90E3"` | true |
| `"1e"` | false |
| `"e3"` | false |
| `"."` | false |
| `".1"` | true |
| `"1."` | true |
| `"+.8"` | true |
| `"46.e3"` | true |
| `"--6"` | false |
| `"-+3"` | false |
| `"95a54e53"` | false |

## Simple function skeleton

This is the minimal skeleton to practice from:

```python
def is_valid_number(s: str) -> bool:
    # TODO: decide whether s represents a valid number.
    return False
```

A manual loop attempt might start like this:

```python
def is_valid_number(s: str) -> bool:
    s = s.strip()
    if not s:
        return False

    for ch in s:
        if ch not in "0123456789+-eE.":
            return False

    # TODO: validate signs, decimal point, exponent, and digit requirements.
    return False
```

The syntax I forgot was the `not in` membership check:

```python
if ch not in "0123456789":
    return False
```

The important correction is that Python uses `not in` as a two-word membership operator. It is not written as:

```python
if ch is not in "0123456789":
    return False
```

## Index-based loop syntax

The Python syntax I forgot for getting both index and character is `enumerate`:

```python
for i, ch in enumerate(s):
    print(i, ch)
```

This is useful when validating signs because `+` / `-` are only valid at the beginning, or immediately after `e` / `E`.

A first instinct was to handle a leading minus by slicing:

```python
if s and s[0] == "-":
    s = s[1:]
```

That can work for a tiny first version, but it misses `+` unless handled too, and it becomes awkward once exponent signs are added, such as `1e-3` or `1e+3`.

## Python regex solution

If the accepted grammar is normal decimal/scientific notation, a compact regex solution is:

```python
import re


def is_valid_number(s: str) -> bool:
    s = s.strip()
    if not s:
        return False

    pattern = r"^[+-]?((\d+(\.\d*)?)|(\.\d+))([eE][+-]?\d+)?$"
    return re.fullmatch(pattern, s) is not None
```

This accepts examples like `"0"`, `"0.1"`, `".1"`, `"1."`, `"+.8"`, `"2e10"`, `"-90E3"`, and `"46.e3"`.

It rejects examples like `""`, `"abc"`, `"1 a"`, `"1e"`, `"e3"`, `"."`, `"--6"`, `"-+3"`, and `"95a54e53"`.

## Concrete mistake log

Problem: valid number parser

Forgot / missed:

- Regex did not come to mind.
- Python `not in` membership-check syntax was not automatic under pressure.
- Python `enumerate(s)` for index-based loops was not automatic under pressure.
- I used/thought of `s = s[1:]` for a leading minus, but did not think about leading plus.
- That slicing approach is okay for a first integer-only version, but it does not scale cleanly to exponent signs like `1e-3`.
- The correct Python membership check is `if ch not in "0123456789":`, not `if ch is not in "0123456789":`.
- I did not think about signs (`+` / `-`) at the beginning.
- I did not think about floating point forms such as `1.`, `.1`, and `+.8` at the beginning.
- I did not think about exponent forms using `e` / `E`, such as `2e10`, `-90E3`, and `46.e3` at the beginning.
- I started coding before pinning down the grammar and examples.
- I did not have a practiced fallback sequence for this kind of validation problem.

Pattern:

- Validation grammar with optional sign, decimal component, and exponent.
- Could be solved with regex or with a finite-state/manual scan.

Recovery habit:

- Do not rely on remembering syntax perfectly.
- First write examples and edge cases.
- Then implement the smallest version.
- If syntax disappears, say calmly: “I’d normally check exact syntax, but I’ll write the intended logic and correct syntax as I go.”

## Practice plan from this postmortem

Turn this exact failure into the first repeated exercise.

Day 0:

- Implement `is_valid_number` in Python.
- Implement `IsValidNumber` in Go.
- Add tests in both languages.
- Write down forgotten syntax.

Day 1:

- Reimplement from scratch without looking at the previous solution for the first 15 minutes.

Day 7:

- Reimplement again.

Day 21:

- Reimplement again under a 30-minute timer, speaking aloud.

## Notes to continue

Add later:

- The exact function skeleton if remembered.
- The exact Python syntax that caused the freeze.
- A clean Python solution.
- A clean Go solution.
- Tests.
- What I would say in an interview next time.

## Reframe

This rejection should not become an identity statement.

The useful conclusion is:

I have strong senior engineering and code review ability, but I need to rebuild the small live-coding muscle enough that artificial interview gates cannot misrepresent me so easily.
